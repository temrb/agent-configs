# Catalog title contract

Status: designated as governing behavior for this exercise by the catalog product owner.

`formatTitle(value)` trims leading and trailing whitespace, converts each non-empty whitespace-separated word to title case, and joins words with one ASCII space. For example, `"  winter SALE "` becomes `"Winter Sale"`. The exported name and one-string-argument interface are compatibility requirements.
