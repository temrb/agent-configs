[project]
name = "missing-audit-fixture-eval"
version = "0.0.0"
requires-python = ">=3.11"

[project.optional-dependencies]
test = ["pytest==8.4.2"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
