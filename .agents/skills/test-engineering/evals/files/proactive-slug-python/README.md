# Route slugs

`slugify` converts a display label into a route segment. The repository currently relies on the examples in the test suite. Product has not approved rules for punctuation, Unicode, repeated whitespace, empty labels, reserved words, maximum length, or collisions.
